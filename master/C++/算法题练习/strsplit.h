#ifndef _STRSPLIT_H_
#define _STRSPLIT_H_
#include <algorithm>
#include <iostream>
#include <string>
#include <vector>
using namespace std;

vector<string> split(string *s, char sep);
// template <typename T>
// auto extend(vector<T> &src, vector<T> &itms) -> decltype(vector<T> &);

#endif
